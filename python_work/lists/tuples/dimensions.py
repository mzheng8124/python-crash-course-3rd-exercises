# example defining a tuple 
dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

# using a for loop to iterate through a tuple
for dimension in dimensions:
    print(dimension)

# assigning the tuple variable to new set of values
dimensions = (400, 100)

# print new tuple
for dimension in dimensions:
    print(dimension)